<div id="form_errors" class="form-errors">
  <% if model.errors.any? %>
    <h2><%= pluralize(model.errors.count, "error") %> prohibited this <%= model.model_name.human %> from being saved:</h2>

    <ul>
      <% model.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  <% end %>
</div>
